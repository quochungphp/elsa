import React from 'react';
import { SignUp } from '../../components/SignUp';
const SignUpView = () => {
    return (
        <React.Fragment>
            <SignUp />
        </React.Fragment>
    );
};

export default SignUpView;
